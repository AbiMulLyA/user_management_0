name: user_management
description: Simple user management app with clean architecture
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # HTTP Request
  http: ^1.1.0

  # State Management
  flutter_bloc: ^8.1.3

  # Utils
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
